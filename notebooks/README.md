# Notebooks
- `00_smoke_test` — Phase 0: check GPU, load small model, embed 3 passages, one retrieval+generate.
- `01_baseline`   — Phase 1: precise baseline end-to-end on the dataset subset.
(Notebooks are added as each phase begins; develop logic in `src/`, call it from thin notebooks.)
